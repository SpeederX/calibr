# calibr roadmap

This file is **archived**. The previous priority-ordered backlog
(v1.1.0 quick wins → v1.2.0 report-interface overhaul → v1.3.0 KV-cache
degradation → v2.0.0 programming-interface landmark → v3.0.0 multi-GPU
landmark) was superseded by the methodology pivot recorded in
[`CLAUDE.md`](CLAUDE.md).

Current direction: ship a Node + Ink CLI wrapping the existing
PowerShell engine (Phase 1), then a NestJS backend (Phase 2), then an
Angular UI (Phase 3). Priorities and the working approach live in
`CLAUDE.md`; concrete next steps live in conversations and open issues.

For the historical backlog (every item, in priority order), see the
git history of this file before the methodology pivot:

```
git show 0d73f11:ROADMAP.md
```
